{
  "name": "@search/types",
  "version": "0.1.0",
 
  "private": true,
  "description": "Define types and interfaces used across the search system",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "dev": "ts-node-dev --respawn src/index.ts",
    "start": "node dist/index.js"
  }
}